__pycache__/
*.py[cod]
*.original.md
.librarian-session/

# Step 1+ build outputs.  The .sqlite form regenerates locally from
# Library_Catalog.json via `catalogue.py --export-sqlite --emit-encoded`
# and is gitignored to avoid binary churn.  The .encoded form (gzip+b64
# wrapped) is the canonical mutable artifact for the claude.ai surface
# and is tracked here so readers can pull from this branch without
# re-running the export.
Library_Catalog.sqlite
dist/
build/

# Library_Catalog.json is deprecated as authoritative on this branch
# (post-Step 6) — SQLite is the source of truth.  The JSON stays as a
# one-time conversion input for `catalogue.py --export-sqlite`, but we
# no longer track changes to it on this branch.  The Code-side workflow
# on `main` continues to track and update it.
Library_Catalog.json
Library_Index.json
Reading_Log.csv
Storygraph.csv
